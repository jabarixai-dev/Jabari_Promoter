Jabari Promoter — Button Navigation Fix

1. Run migration.sql in Supabase SQL Editor.
2. Replace bot.js in GitHub with the included bot.js.
3. Commit the change.
4. Manually deploy the latest commit on Render.
5. Do not change your existing Render environment variables.

The bot supports multiple saved campaigns and one active campaign. Creating a new campaign does not require deleting the active one.
